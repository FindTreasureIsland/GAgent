# GAgent 忽略规则 / GAgent Ignore Rules

# 依赖目录 / Dependencies
node_modules/

# 构建产物 / Build Outputs
**/dist/
build/
coverage/

# TypeScript 构建信息 / TypeScript Build Info
*.tsbuildinfo

# 日志与调试文件 / Logs & Debug Files
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache
.node_repl_history

# 操作系统与编辑器缓存 / OS & Editor Cache
.DS_Store
Thumbs.db
*.swp
.idea/

# 环境配置文件（含本地变体） / Environment Files (including local variants)
.env
.env.*
.env.local
.env.development.local
.env.test.local
.env.production.local

# 临时目录 / Temporary
tmp/

# Web Server 运行时产生的遥测数据 / Web Server runtime telemetry
packages/web-server/telemetry.jsonl

# 保留在版本库中的目录（不要忽略） / Keep tracked (do NOT ignore)
# bundle/ 目录包含沙箱配置与预打包脚本，需保留 / bundle includes sandbox configs and pre-bundled scripts
# bundle/